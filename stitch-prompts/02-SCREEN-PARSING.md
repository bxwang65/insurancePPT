# Screen 2: Parsing（解析中页）

> 把这个文件直接复制粘贴到 Google Stitch 的 prompt 输入框。
> 输出目标：单个 HTML 文件。

---

## Prompt（直接复制以下全部内容）

```
请生成一个 HTML 单页应用，主题是"保险计划书 AI 助手"的解析中页。
用户已经上传了 PDF，正在等待 AI 解析完成。本屏展示进度、状态、可取消。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【一】请严格遵循以下设计系统（来自 00-DESIGN-SYSTEM.md）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

产品定位: 「把厚重的 PDF 计划书，变成有温度的专业销售演示文稿」
目标用户: 保险销售顾问
情绪基调: 克制、专业、可信、温暖（解析中应该让用户感到"事情在推进"，不焦虑）

【配色 - 白底 Apple 风】
  背景基色:      #FAFAFA
  卡片背景:      #FFFFFF
  分隔线/边线:   #E5E5EA
  纯黑标题:      #1D1D1F
  次要文字:      #6E6E73
  三级文字/占位: #AEAEB2
  品牌金:        #C8963E
  品牌金浅底:    #FBF6EC
  成功: #34C759 / 警告: #FF9500 / 错误: #FF3B30 / 处理中: #007AFF

  ❌ 严禁深海蓝背景、紫色渐变

【字体】 -apple-system, "SF Pro Display", "Inter", "PingFang SC", sans-serif
  数字: "SF Mono", monospace
  字号阶梯: display 56 / h1 40 / h2 28 / h3 20 / body-lg 17 / body 15 / caption 13 / micro 11
  ALL CAPS 必须 letter-spacing: 0.06em+

【圆角】 xs 6 / sm 10 / md 14 / lg 20 / xl 28
【阴影】 极轻，最大 rgba(0,0,0,0.10)
【间距】 8 栅格
【图标】 Lucide 风格 SVG，禁止 emoji
  本屏用到: file-text, check-circle-2, loader (旋转 SVG), x, sparkles
【动效】 入场 fade 300ms / 进度条平滑 600ms / 旋转 loader 800ms linear infinite
【布局】 桌面优先 1120px, 移动端 16px 内边距

【严禁】 任何 emoji / 深海蓝紫 / 浮夸阴影 / 模板化 AI 文案

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【二】本屏的具体需求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【1. 顶部导航栏 (与 Screen 1 完全一致)】
  - 高度 64px, 白底, 底边 1px
  - 左侧 logo (28x28 金色圆角方块 + "IP" + "Insurance Plan AI")
  - 中间 5 步指示器: 上传✓ → 解析(激活) → 对话 → 生成 → 完成
    * 已完成步骤显示绿色 check-circle-2 图标 (颜色 #34C759)
    * 当前第 2 步: 实心黑色圆 + 数字 "2" + 文字"解析"
    * 后续步骤灰色
  - 右侧: "取消并返回" 文字按钮 (#6E6E73, 13px)

【2. 主区 - 居中单列布局, 上下结构】
  - 上方 (主视觉/进度): 一张大白卡, 圆角 20, 阴影 shadow-md, 内边距 48px
  - 下方 (文件列表): 一组小白卡, 圆角 16, 阴影 shadow-sm, 内边距 20px

【3. 主卡 (上方) - 解析进度总览】
  - 高度自适应, 居中对齐
  - 顶部: 一个动态可视化区
    * 中央一个 120x120 的圆形进度环
    * 外环: 12px 描边, 颜色 #E5E5EA
    * 内环 (进度): 12px 描边, 颜色 #C8963E
    * 进度从 0 增长到 100%, 圆滑过渡
    * 圆环内部: 大数字 "65%" (40px / 600, 颜色 #1D1D1F) + 小字 "OVERALL" (11px / 600,
      letter-spacing 0.08em, 颜色 #6E6E73)
    * 圆环右侧 24px 处: 一个旋转的 sparkles SVG 图标 (32px, 颜色 #C8963E,
      1.2s linear infinite)
  - 中部: 阶段文字 (居中, 上下内边距 32px)
    * h2: "AI 正在深度阅读您的计划书" (28px / 600, #1D1D1F)
    * body-lg (#6E6E73, 17px): "提取数据 → 理解产品设计 → 生成销售洞察"
    * 三个阶段用一行小标签表示, 当前阶段的标签背景 #FBF6EC, 文字 #C8963E
      (其他两个标签背景 #F5F5F7, 文字 #6E6E73)
  - 底部: 当前处理文件名 (居中)
    * 文字 "正在处理: 守护家倍198-终版.pdf" (15px / 500, #1D1D1F)
    * 旁边一个 loader 小图标 (16px, 旋转, #007AFF)
    * 下方一个 "X / 3" 进度 (13px / 400, #6E6E73)

【4. 文件列表 (主卡下方)】
  - 横向/纵向自适应, 桌面端纵向 1 列 (每行一个文件, 3 行)
  - 移动端纵向 1 列
  - 每行 (一个文件卡):
    * 圆角 16, 背景 #FFFFFF, 边框 1px solid #E5E5EA, 内边距 20px
    * 左侧: file-text 图标 (24px, 颜色 #6E6E73)
    - 旁边一个产品类型小徽章 (圆角 6, 高度 22px, padding 0 8px, 字号 11px / 600)
      * 储蓄险: 背景 #E8F1FF, 文字 #007AFF
      * 重疾险: 背景 #FFF0F0, 文字 #FF3B30
      * 万用寿险: 背景 #F0FFF4, 文字 #34C759
    * 中间: 文件名 (15px / 500, #1D1D1F) + 副信息 "2.3 MB · 12 页" (13px, #6E6E73)
    * 右侧 (状态区, 宽度固定 200px):
      - pending 态: 文字 "等待中" (13px / 500, #AEAEB2) + 灰色横线占位
      - parsing 态: 圆环 + 百分比 (40px 直径, 12px 描边, #E5E5EA 外, #C8963E 内)
        + 旁边 "解析中" (13px / 500, #C8963E)
      - success 态: check-circle-2 图标 (20px, #34C759) + "已完成" (13px / 500, #34C759)
      - fail 态: x 圆形图标 (20px, #FF3B30) + "失败" (13px / 500, #FF3B30) +
        一行 12px 红色错误提示文字
    * 整体行高 60px
  - 文件之间间隔 12px

【5. 状态切换的微交互】
  - 文件状态从 pending → parsing 时: 整行背景从 #FFFFFF 变为 #FBF6EC (浅金),
    圆环 loader 旋转, 200ms 过渡
  - 从 parsing → success 时: 行背景变白, 圆环淡出, check 图标带 0.3s 缩放入场 (从 0.6 变 1)
  - 从 parsing → fail 时: 行背景保持 #FFFAFA (浅红), 错误图标淡入

【6. 底部固定操作栏 (仅"取消"按钮, 解析中主操作禁用)】
  - 高度 88px, 白底, 顶边 1px, 阴影 shadow-lg
  - 左侧: "已用 18 秒 · 预计还需 12 秒" (15px / 500, #1D1D1F)
  - 右侧: 一个次按钮 "取消解析" (白底, 边框 #E5E5EA, 文字 #1D1D1F, 高度 48px,
    内边距 0 24px, 圆角 14)
  * 解析完成时此按钮变成主按钮 "继续 →" (背景 #C8963E, 文字 #FFFFFF)

【7. 解析完成的过渡态 (特殊)】
  - 解析全部成功时, 主卡的"AI 正在深度阅读"标题变为
    "解析完成!" (28px / 600, #1D1D1F), 副标题变为
    "我们已提取出所有关键数据, 可以开始下一步了" (#6E6E73)
  - 主卡顶部圆环变成实心绿色 (背景 #34C759) + 内部白色 check 大图标 (48px)
  - 持续 600ms 庆祝动画 (圆环从内向外散出一圈淡金色光晕, 然后淡出)
  - 底部按钮从 "取消" 切换为 "继续 →" (金色主按钮)

【8. 移动端降级】
  - 圆环尺寸从 120 缩到 96
  - 文件列表的右侧状态区宽度从 200 缩到 140, 文字字号缩小
  - 底部操作栏在移动端高度 72px, 按钮文字缩短

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【三】技术要求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 单一 HTML 文件, 内联 CSS 和 vanilla JS
2. 使用 Tailwind CSS via CDN
3. JS 必须实现:
   a) 模拟解析流程: 页面打开时启动一个 setInterval, 每 800ms 推进一个文件的
      pending → parsing → success 状态 (总耗时 6-9 秒)
   b) 主卡圆环进度 = 已完成文件数 / 总文件数 * 100
   c) 阶段文字 "提取数据" → "理解产品设计" → "生成销售洞察" 在解析中按文件
      进度切换 (33% / 66% / 100% 时切)
   d) "取消解析" 按钮: 弹一个确认对话框 (用浏览器原生 confirm 即可),
      确认后清空所有状态, 触发一个 window.parent.postMessage('cancel', '*')
      (占位, 实际联调时替换)
   e) 解析全部完成时, "继续 →" 按钮 800ms 后变为激活态 (模拟网络延迟)
4. 圆环进度用 SVG circle + stroke-dasharray 动画, 不要用 div
5. 所有图标 SVG 内联
6. 键盘可访问, focus 态金色 outline

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【四】不要做的事
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 不要生成超过 1 个 HTML 文件
- 不要使用任何 JS 框架
- 不要引入外部图片
- 不要写后端代码
- 不要有侧边栏/侧边导航
- 不要有 dark mode
- 不要有多语言
- 不要做真实网络请求 (用 setInterval 模拟即可)
- 不要有拖拽上传功能
- 不要在解析中显示"预计剩余时间"的精确数字 (说"预计还需约 X 秒"即可)
```
